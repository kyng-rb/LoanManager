# Please refer https://aka.ms/HTTPSinContainer on how to setup an https developer certificate for your ASP.NET Core service.

version: '3.4'

services:
  api:
    image: loanmanagerpresentationapi
    build:
      context: .
      dockerfile: src/LoanManager.Presentation.API/Dockerfile
    ports:
      - 80:5001
